/**
 * What the guide's button says, which is a question mark in every language.
 *
 * The one mark for "what does this mean" that needs no translation, and the only
 * one that fits where it is: a word in that corner would be a second pill of
 * writing over the sky, stacked on the console's. Kept apart from `GuideToggle`
 * for the reason `CONSOLE_LABEL` is kept apart from its button — the intro's
 * page about the corners draws a copy of the badge, and the pages are data that
 * should not have to import a component to say what a button reads.
 */
export const GUIDE_LABEL = "?";
