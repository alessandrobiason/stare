/**
 * What the app is called, in one place.
 *
 * It is a proper name and so it is not in `src/i18n` — there is no Italian for
 * "Stare" any more than there is for "Starlink" (see `STARLINK_LABEL`). It is
 * written twice on the way into the sky view: letter-spaced and upper-cased on
 * the boot screen, where it is the only thing on the screen, and in the corner
 * of the sky view itself, where it is a title over the count of what is up
 * there. Both read it from here so the two cannot drift apart.
 */
export const APP_NAME = "Stare";
